name: macaron-stack
type: nodejs:18
hooks:
  build: |
    cd feathers-basics
    ./fastly-install.sh
    npm install
  post_deploy:
    PATH=$PATH:/app/.fastly
web:
  commands:
    start: |
      npx ts-node app.ts
disk: 512
relationships:
  pgdb: "postgresqldb:postgresql"
variables:
  env:
    FASTLY_API_TOKEN: abcdefghi
mounts:
  '.config/fastly':
    source: local
    source_path: fastly